# Moved

Semantic layer YAML is under **[`models/semantic/`](../models/semantic/README.md)**.

- dbt 1.11: no `semantic-models-paths` in `dbt_project.yml`
- MetricFlow requires **`models/semantic/time_spine_daily`** — see `_time_spine.yml`
