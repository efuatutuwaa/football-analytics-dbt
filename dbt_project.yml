name: 'football_analytics'
version: '1.0.0'
profile: 'football_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  football_analytics:
    staging:
      +materialized: view
      +schema: football_staging
      stg_fixtures:
        +materialized: table
      stg_fixture_events:
        +materialized: table
      stg_fixture_lineups_players:
        +materialized: table
      stg_fixture_statistics:
        +materialized: table
      stg_player_statistics:
        +materialized: table
      stg_standings:
        +materialized: table
    intermediate:
      +materialized: table
      +schema: football_intermediate
    core:
      +materialized: table
      +schema: football_core
    marts:
      +materialized: table
      +schema: football_marts
    ops:
      +materialized: table
      +schema: football_ops

snapshots:
  football_analytics:
    +target_schema: football_core
